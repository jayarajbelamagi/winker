# Production: set in Vercel → Settings → Environment Variables (all environments you use).
# Must match your Render backend origin, no trailing slash.
# Example: https://twitter-clone-backend-xxxx.onrender.com
#
# Leave empty for local dev — Vite proxies /api → http://localhost:5000
VITE_API_URL=
